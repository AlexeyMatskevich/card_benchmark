# frozen_string_literal: true

class CreditCard < Sequel::Model
  many_to_one :user
end

# Table: credit_cards
# Columns:
#  id      | integer | PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY
#  user_id | integer |
#  color   | text    | NOT NULL
# Indexes:
#  credit_cards_pkey | PRIMARY KEY btree (id)
# Foreign key constraints:
#  credit_cards_user_id_fkey | (user_id) REFERENCES users(id)
