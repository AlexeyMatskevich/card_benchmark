# frozen_string_literal: true

class DebitCard < Sequel::Model
  many_to_one :user
end

# Table: debit_cards
# Columns:
#  id      | integer | PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY
#  user_id | integer |
#  color   | text    | NOT NULL
#  count   | integer | NOT NULL
#  meta    | jsonb   |
# Indexes:
#  debit_cards_pkey | PRIMARY KEY btree (id)
# Foreign key constraints:
#  debit_cards_user_id_fkey | (user_id) REFERENCES users(id)
